{{!< main}}

<div class="card">
    <div class="card-header">
        <h5 class="card-title">Añadir Stock - {{ product.name }}</h5>
    </div>
    <div class="card-body">
        <form action="/products/{{ product._id }}/add-stock" method="POST">
            <div class="mb-3">
                <label for="quantity" class="form-label">Cantidad a Añadir</label>
                <input type="number" class="form-control" id="quantity" name="quantity" required>
            </div>
            <button type="submit" class="btn btn-primary">Añadir Stock</button>
        </form>
    </div>
</div>
