<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inicio</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/5.0.0-alpha2/css/bootstrap.min.css">
    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <style>
        body {
            background-color: #f0f0f0; /* Color pastel de fondo */
            padding-top: 4rem;
            transition: background-color 0.3s ease;
        }
        .custom-bubble {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            font-weight: bold;
            color: white;
            margin: 1rem;
            cursor: pointer;
            transition: transform 0.2s ease-in-out;
        }
        .custom-bubble:hover {
            transform: scale(1.1);
        }
        .blue {
            background-color: #89CFF0; /* Azul pastel */
        }
        .pink {
            background-color: #FF9AA2; /* Rosa pastel */
        }
        .green {
            background-color: #77DD77; /* Verde pastel */
        }
        .purple {
            background-color: #B39CD0; /* Púrpura pastel */
        }
        /* Modo oscuro */
        .dark-mode body {
            background-color: #333; /* Fondo oscuro */
            color: #fff; /* Texto blanco */
        }
        .dark-mode .custom-bubble {
            background-color: #555; /* Color de burbuja en modo oscuro */
        }
    </style>
</head>
<body>
    <div class="container text-center">
        <h1 class="mb-4">Opciones</h1>
        <div class="row justify-content-center">
            <div class="col-lg-3 col-md-4 col-sm-6">
                <div class="custom-bubble blue" onclick="location.href='/products'">
                    <i class="fas fa-box-open"></i>
                </div>
                <p>Ver Productos</p>
            </div>
            <div class="col-lg-3 col-md-4 col-sm-6">
                <div class="custom-bubble pink" onclick="location.href='/products/add'">
                    <i class="fas fa-plus"></i>
                </div>
                <p>Agregar Producto</p>
            </div>
            <div class="col-lg-3 col-md-4 col-sm-6">
                <div class="custom-bubble green" onclick="location.href='/about'">
                    <i class="fas fa-info-circle"></i>
                </div>
                <p>Acerca de</p>
            </div>
            <div class="col-lg-3 col-md-4 col-sm-6">
                <div class="custom-bubble purple" onclick="location.href='/contact'">
                    <i class="fas fa-envelope"></i>
                </div>
                <p>Contacto</p>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.0.0-alpha2/js/bootstrap.bundle.min.js"></script>
    <script src="https://kit.fontawesome.com/a076d05399.js"></script>
    <script>
        function toggleDarkMode() {
            const body = document.querySelector('body');
            body.classList.toggle('dark-mode');
        }
    </script>
</body>
</html>
